export class MobileSend {
    phone: [number];
    message: string;
    constructor(
        phone: [number],
        message: string,
    ) {
        this.phone = phone;
        this.message = message;
    }
}
