export class SmsSelect {
    id?: string;
    mobileNumber?: string;
    constructor(
        id?: string,
        mobileNumber?: string
    ) {
        this.id = id;
        this.mobileNumber = mobileNumber;
    }
}
